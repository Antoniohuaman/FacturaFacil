name = "pm-portal"
pages_build_output_dir = "./apps/pm-portal/dist"
compatibility_date = "2026-03-07"

# Pages Functions se descubren automaticamente en ./functions
# porque este archivo vive en la raiz del repo.
# Secretos locales para wrangler pages dev: usar ./.dev.vars y no commitearlo.

[dev]
ip = "localhost"
port = 8788
local_protocol = "http"